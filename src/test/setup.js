// import { afterAll, afterEach, beforeAll } from 'vitest';
// import { server } from './server.js';

// 모든 테스트 전에 서버를 시작합니다.
// beforeAll(() => server.listen());

// // 각 테스트 후에 요청 핸들러를 리셋합니다.
// afterEach(() => server.resetHandlers());

// // 모든 테스트 후에 서버를 종료합니다.
// afterAll(() => server.close());
